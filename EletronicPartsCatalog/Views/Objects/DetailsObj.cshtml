@{
    ViewData["Title"] = "Detalhes do componente";
}
@model EletronicPartsCatalog.Web.Pages.Models.ObjectViewModel;

@section Styles {
    <link href="~/dist/styles/parts.css" rel="stylesheet" />
}

<div class="row">
    <div class="col-md-8 col-md-push-2">
        <section>
            <h4>Detalhes de Componente</h4>
            <hr />
            <div class="ObjectsListItem row">
                <div class="col-md-12">
                    <div class="ObjectsListItem-summary row">
                        <div class="col-md-6 padding-none">
                            <span class="ObjectsListItem-name">@Model.Name</span>
                        </div>
                        <div class="col-md-6 padding-none">
                            <span class="pull-right">
                                <span class="glyphicon glyphicon-calendar"></span> @Model.CreationDate
                            </span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-12 padding-none">
                            <span class="ObjectsListItem-description">@Model.Description</span>
                        </div>
                    </div>
                </div>
            </div>
            <form action="/Objects/Delete/@Model.Id" method="post">
                @Html.AntiForgeryToken()

                <button type="submit" class="btn btn-danger pull-right">
                    <span class="glyphicon glyphicon-remove"></span> Deletar esse componente
                </button>
            </form>
        </section>
    </div>
</div>